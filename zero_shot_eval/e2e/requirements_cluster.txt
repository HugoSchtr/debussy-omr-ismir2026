# Requirements for cluster training
# Install with: pip install -r requirements_cluster.txt
# Note: PyTorch 2.0.x is required for GTX 1080 Ti (CUDA compute capability 6.1)

torch>=2.0.0,<2.1.0
torchvision>=0.15.0,<0.16.0
lightning>=2.0.0,<2.1.0
opencv-python>=4.8.0,<4.10.0
numpy>=1.24.0,<2.0.0
pillow>=10.0.0
loguru>=0.7.0
rich>=13.0.0
gin-config>=0.5.0
torchinfo>=1.8.0
datasets>=2.14.0  # For HuggingFace dataset loading

# Optional: Weights & Biases for experiment tracking
wandb>=0.15.0
fire>=0.5.0
